# sheets.banco Web — packages/web/.env.local (consumed by Next.js)
# Copy to `.env.local` and fill in.

# Where the API lives. In local dev with `npm run dev`, the API runs on
# :3000 and Next.js on :3001 so they don't collide.
NEXT_PUBLIC_API_URL=http://localhost:3000
