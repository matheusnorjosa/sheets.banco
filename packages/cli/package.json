{
  "name": "sheets-banco-cli",
  "version": "0.1.0",
  "description": "CLI for sheets.banco — turn Google Sheets into REST APIs",
  "type": "module",
  "bin": {
    "sheets-banco": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/cli.ts --format esm --dts --clean",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "google-sheets",
    "api",
    "cli"
  ],
  "license": "MIT",
  "dependencies": {
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}
